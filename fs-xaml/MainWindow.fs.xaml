<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Title" Height="600" Width="800">

    <Window.Resources>
        <ResourceDictionary Source="/fs_xaml;component/Resource.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="txtInput" VerticalAlignment="Top"/>

        <Button x:Name="btnClick" Content="Click" Grid.Row="1"/>
    </Grid>
</Window>